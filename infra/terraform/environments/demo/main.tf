# Checkpoint A intentionally defines no AWS resources.
#
# Resource modules will be added in later checkpoints after the current
# App Runner and GitHub Pages deployments remain verified as rollback targets.
