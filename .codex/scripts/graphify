#!/usr/bin/env bash

set -euo pipefail

if [ -n "${GRAPHIFY_BIN:-}" ]; then
  exec "$GRAPHIFY_BIN" "$@"
fi

if [ -x "$HOME/.local/bin/graphify" ]; then
  exec "$HOME/.local/bin/graphify" "$@"
fi

exec graphify "$@"
